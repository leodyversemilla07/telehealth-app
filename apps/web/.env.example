# =============================================================================
# Telehealth App — environment variables moved to the workspace root.
# =============================================================================
# There is a single root `.env` (gitignored) for the whole monorepo, loaded by
# `@telehealth/env/load` (see apps/web/next.config.mjs `loadRootEnv()`). See
# the root `.env.example` — copy it to `.env` at the repo root and fill in
# real values. This file exists only for old links.
